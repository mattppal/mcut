---
title: "renderFrame"
description: Generated TypeScript API reference for the public mcut SDK.
---

[**mcut SDK**](../../../index.mdx)

***

[mcut SDK](../../../index.mdx) / [compositor/src](../index.mdx) / renderFrame

# Function: renderFrame()

```ts
function renderFrame(
   ctx, 
   project, 
   timeMs, 
   options?): void;
```

Defined in: [packages/compositor/src/render-frame.ts:26](https://github.com/mattppal/mcut/blob/main/packages/compositor/src/render-frame.ts#L26)

Render one frame of `project` at `timeMs` into `ctx` (canvas2d path).

Pure with respect to inputs: the same project, time, and frame source
produce the same pixels — which is what makes the export path
deterministic. The context is expected to be in project coordinates
(`project.width` × `project.height`); callers rendering at other sizes
apply their own transform before calling.

## Parameters

### ctx

[`Canvas2D`](../type-aliases/Canvas2D.mdx)

### project

#### assets

`z.ZodDefault`\<`z.ZodRecord`\<`z.ZodString`, `z.ZodObject`\<\{
  `durationMs`: `z.ZodOptional`\<`z.ZodNumber`\>;
  `hash`: `z.ZodOptional`\<`z.ZodString`\>;
  `height`: `z.ZodOptional`\<`z.ZodNumber`\>;
  `id`: `z.ZodCustom`\<`` `a-${string}` ``, `` `a-${string}` ``\>;
  `kind`: `z.ZodEnum`\<\{
     `audio`: `"audio"`;
     `image`: `"image"`;
     `video`: `"video"`;
  \}\>;
  `mimeType`: `z.ZodOptional`\<`z.ZodString`\>;
  `name`: `z.ZodOptional`\<`z.ZodString`\>;
  `nativePreview`: `z.ZodOptional`\<`z.ZodBoolean`\>;
  `src`: `z.ZodString`;
  `width`: `z.ZodOptional`\<`z.ZodNumber`\>;
\}, `z.core.$strip`\>\>\>

#### fps

`number`

#### height

`number`

#### id

`string`

#### layouts

`object`[]

#### markers

`object`[]

#### name

`string`

#### presets

`object`[]

#### tracks

`object`[]

#### version

`1`

#### width

`number`

### timeMs

`number`

### options?

[`RenderFrameOptions`](../interfaces/RenderFrameOptions.mdx) = `{}`

## Returns

`void`
