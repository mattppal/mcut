---
title: "getFitScale"
description: Generated TypeScript API reference for the public mcut SDK.
---

[**mcut SDK**](../../../index.mdx)

***

[mcut SDK](../../../index.mdx) / [compositor/src](../index.mdx) / getFitScale

# Function: getFitScale()

```ts
function getFitScale(
   project, 
   width, 
   height): number;
```

Defined in: [packages/compositor/src/geometry.ts:200](https://github.com/mattppal/mcut/blob/main/packages/compositor/src/geometry.ts#L200)

"Contain" scale factor for fitting a `width`×`height` media into the
project frame (used when inserting media elements).

## Parameters

### project

#### assets

`z.ZodDefault`\<`z.ZodRecord`\<`z.ZodString`, `z.ZodObject`\<\{
  `durationMs`: `z.ZodOptional`\<`z.ZodNumber`\>;
  `hash`: `z.ZodOptional`\<`z.ZodString`\>;
  `height`: `z.ZodOptional`\<`z.ZodNumber`\>;
  `id`: `z.ZodCustom`\<`` `a-${string}` ``, `` `a-${string}` ``\>;
  `kind`: `z.ZodEnum`\<\{
     `audio`: `"audio"`;
     `image`: `"image"`;
     `video`: `"video"`;
  \}\>;
  `mimeType`: `z.ZodOptional`\<`z.ZodString`\>;
  `name`: `z.ZodOptional`\<`z.ZodString`\>;
  `nativePreview`: `z.ZodOptional`\<`z.ZodBoolean`\>;
  `src`: `z.ZodString`;
  `width`: `z.ZodOptional`\<`z.ZodNumber`\>;
\}, `z.core.$strip`\>\>\>

#### fps

`number`

#### height

`number`

#### id

`string`

#### layouts

`object`[]

#### markers

`object`[]

#### name

`string`

#### presets

`object`[]

#### tracks

`object`[]

#### version

`1`

#### width

`number`

### width

`number`

### height

`number`

## Returns

`number`
