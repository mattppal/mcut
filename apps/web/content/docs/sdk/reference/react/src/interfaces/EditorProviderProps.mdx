---
title: "EditorProviderProps"
description: Generated TypeScript API reference for the public mcut SDK.
---

[**mcut SDK**](../../../index.mdx)

***

[mcut SDK](../../../index.mdx) / [react/src](../index.mdx) / EditorProviderProps

# Interface: EditorProviderProps

Defined in: [packages/react/src/context.tsx:14](https://github.com/mattppal/mcut/blob/main/packages/react/src/context.tsx#L14)

## Properties

### children

```ts
children: ReactNode;
```

Defined in: [packages/react/src/context.tsx:20](https://github.com/mattppal/mcut/blob/main/packages/react/src/context.tsx#L20)

***

### engine?

```ts
optional engine?: EditorEngine;
```

Defined in: [packages/react/src/context.tsx:16](https://github.com/mattppal/mcut/blob/main/packages/react/src/context.tsx#L16)

Bring your own engine (e.g. created outside React); otherwise one is created.

***

### maxHistorySize?

```ts
optional maxHistorySize?: number;
```

Defined in: [packages/react/src/context.tsx:19](https://github.com/mattppal/mcut/blob/main/packages/react/src/context.tsx#L19)

***

### project?

```ts
optional project?: object;
```

Defined in: [packages/react/src/context.tsx:18](https://github.com/mattppal/mcut/blob/main/packages/react/src/context.tsx#L18)

Initial project for the internally-created engine.

#### assets

```ts
assets: Record<string, {
  durationMs?: number;
  hash?: string;
  height?: number;
  id: `a-${string}`;
  kind: "video" | "audio" | "image";
  mimeType?: string;
  name?: string;
  nativePreview?: boolean;
  src: string;
  width?: number;
}>;
```

#### fps

```ts
fps: number;
```

#### height

```ts
height: number;
```

#### id

```ts
id: string;
```

#### layouts

```ts
layouts: object[];
```

Multicam layout templates this project references. See layouts.ts.

#### markers

```ts
markers: object[];
```

Timeline markers, sorted by `timeMs` (the commands keep them sorted).

#### name

```ts
name: string;
```

#### presets

```ts
presets: object[];
```

Named inspector value bundles (style presets). See presets.ts.

#### tracks

```ts
tracks: object[];
```

Render order: index 0 is painted first (bottom); last is topmost.

#### version

```ts
version: 1;
```

Project format version (see `migrations.ts`). `parseProject` migrates
older documents before validating; the default covers in-memory creation.

#### width

```ts
width: number;
```
