---
title: "SilenceCutPlan"
description: Generated TypeScript API reference for the public mcut SDK.
---

[**mcut SDK**](../../../index.mdx)

***

[mcut SDK](../../../index.mdx) / [editor/src](../index.mdx) / SilenceCutPlan

# Interface: SilenceCutPlan

Defined in: [packages/editor/src/silence-cuts.ts:35](https://github.com/mattppal/mcut/blob/main/packages/editor/src/silence-cuts.ts#L35)

## Properties

### commands

```ts
commands: AnyCommand[];
```

Defined in: [packages/editor/src/silence-cuts.ts:39](https://github.com/mattppal/mcut/blob/main/packages/editor/src/silence-cuts.ts#L39)

Every command dispatched, in order; replayable on the input project.

***

### project

```ts
project: object;
```

Defined in: [packages/editor/src/silence-cuts.ts:42](https://github.com/mattppal/mcut/blob/main/packages/editor/src/silence-cuts.ts#L42)

The project with all cuts applied.

#### assets

```ts
assets: Record<string, {
  durationMs?: number;
  hash?: string;
  height?: number;
  id: `a-${string}`;
  kind: "video" | "audio" | "image";
  mimeType?: string;
  name?: string;
  nativePreview?: boolean;
  src: string;
  width?: number;
}>;
```

#### fps

```ts
fps: number;
```

#### height

```ts
height: number;
```

#### id

```ts
id: string;
```

#### layouts

```ts
layouts: object[];
```

Multicam layout templates this project references. See layouts.ts.

#### markers

```ts
markers: object[];
```

Timeline markers, sorted by `timeMs` (the commands keep them sorted).

#### name

```ts
name: string;
```

#### presets

```ts
presets: object[];
```

Named inspector value bundles (style presets). See presets.ts.

#### tracks

```ts
tracks: object[];
```

Render order: index 0 is painted first (bottom); last is topmost.

#### version

```ts
version: 1;
```

Project format version (see `migrations.ts`). `parseProject` migrates
older documents before validating; the default covers in-memory creation.

#### width

```ts
width: number;
```

***

### removedMs

```ts
removedMs: number;
```

Defined in: [packages/editor/src/silence-cuts.ts:40](https://github.com/mattppal/mcut/blob/main/packages/editor/src/silence-cuts.ts#L40)

***

### silences

```ts
silences: SilenceWindow[];
```

Defined in: [packages/editor/src/silence-cuts.ts:37](https://github.com/mattppal/mcut/blob/main/packages/editor/src/silence-cuts.ts#L37)

The cuts, in source-media time, padding already applied.
