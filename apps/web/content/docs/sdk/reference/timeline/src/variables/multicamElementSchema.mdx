---
title: "multicamElementSchema"
description: Generated TypeScript API reference for the public mcut SDK.
---

[**mcut SDK**](../../../index.mdx)

***

[mcut SDK](../../../index.mdx) / [timeline/src](../index.mdx) / multicamElementSchema

# Variable: multicamElementSchema

```ts
const multicamElementSchema: ZodObject<{
  angles: ZodArray<ZodObject<{
     atMs: ZodNumber;
     layoutId: ZodString;
  }, $strip>>;
  angleTransition: ZodOptional<ZodObject<{
     durationMs: ZodDefault<ZodNumber>;
     type: ZodString;
  }, $strip>>;
  audioSource: ZodOptional<ZodString>;
  blendMode: ZodOptional<ZodEnum<{
     color: "color";
     color-burn: "color-burn";
     color-dodge: "color-dodge";
     darken: "darken";
     difference: "difference";
     exclusion: "exclusion";
     hard-light: "hard-light";
     hue: "hue";
     lighten: "lighten";
     luminosity: "luminosity";
     multiply: "multiply";
     normal: "normal";
     overlay: "overlay";
     saturation: "saturation";
     screen: "screen";
     soft-light: "soft-light";
  }>>;
  durationMs: ZodNumber;
  effects: ZodOptional<ZodArray<ZodType<Effect, unknown, $ZodTypeInternals<Effect, unknown>>>>;
  fadeInMs: ZodOptional<ZodNumber>;
  fadeOutMs: ZodOptional<ZodNumber>;
  id: ZodCustom<`e-${string}`, `e-${string}`>;
  keyframes: ZodOptional<ZodRecord<ZodEnum<{
     blur: "blur";
     letterSpacing: "letterSpacing";
     opacity: "opacity";
     position.x: "position.x";
     position.y: "position.y";
     rotation: "rotation";
     scale.x: "scale.x";
     scale.y: "scale.y";
     volume: "volume";
   }> & $partial, ZodArray<ZodObject<{
     easing: ZodOptional<ZodUnion<readonly [ZodEnum<...>, ZodObject<..., ...>]>>;
     timeMs: ZodNumber;
     value: ZodNumber;
  }, $strip>>>>;
  linkId: ZodOptional<ZodString>;
  motionBlur: ZodOptional<ZodObject<{
     enabled: ZodDefault<ZodBoolean>;
     shutterAngle: ZodDefault<ZodNumber>;
  }, $strip>>;
  muted: ZodDefault<ZodBoolean>;
  opacity: ZodDefault<ZodNumber>;
  sources: ZodArray<ZodObject<{
     assetId: ZodCustom<`a-${string}`, `a-${string}`>;
     key: ZodString;
     trimStartMs: ZodDefault<ZodNumber>;
  }, $strip>>;
  startMs: ZodNumber;
  timeMap: ZodOptional<ZodArray<ZodObject<{
     easing: ZodOptional<ZodUnion<readonly [ZodEnum<{
        easeIn: ...;
        easeInOut: ...;
        easeOut: ...;
        hold: ...;
        linear: ...;
      }>, ZodObject<{
        cubicBezier: ...;
     }, $strip>]>>;
     timeMs: ZodNumber;
     value: ZodNumber;
  }, $strip>>>;
  transform: ZodPrefault<ZodObject<{
     rotation: ZodDefault<ZodNumber>;
     scaleX: ZodDefault<ZodNumber>;
     scaleY: ZodDefault<ZodNumber>;
     x: ZodDefault<ZodNumber>;
     y: ZodDefault<ZodNumber>;
  }, $strip>>;
  transition: ZodOptional<ZodObject<{
     durationMs: ZodDefault<ZodNumber>;
     type: ZodString;
  }, $strip>>;
  type: ZodLiteral<"multicam">;
  volume: ZodDefault<ZodNumber>;
}, $strip>;
```

Defined in: [packages/timeline/src/model.ts:344](https://github.com/mattppal/mcut/blob/main/packages/timeline/src/model.ts#L344)
