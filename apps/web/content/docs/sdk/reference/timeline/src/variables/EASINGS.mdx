---
title: "EASINGS"
description: Generated TypeScript API reference for the public mcut SDK.
---

[**mcut SDK**](../../../index.mdx)

***

[mcut SDK](../../../index.mdx) / [timeline/src](../index.mdx) / EASINGS

# Variable: EASINGS

```ts
const EASINGS: object;
```

Defined in: [packages/timeline/src/animation-presets.ts:76](https://github.com/mattppal/mcut/blob/main/packages/timeline/src/animation-presets.ts#L76)

The canonical pro easing vocabulary as cubic-beziers (easings.net /
Material 3 values — the same curves Flow and GSAP ship). Shared by presets
and exposed for graph-editor style UIs.

## Type Declaration

### emphasizedAccelerate

```ts
emphasizedAccelerate: object;
```

Material 3 emphasized-accelerate: exit-screen moves.

#### emphasizedAccelerate.cubicBezier

```ts
cubicBezier: [number, number, number, number];
```

### inBack

```ts
inBack: object;
```

Anticipation: winds up backwards before leaving.

#### inBack.cubicBezier

```ts
cubicBezier: [number, number, number, number];
```

### inCubic

```ts
inCubic: object;
```

Default accelerating exit.

#### inCubic.cubicBezier

```ts
cubicBezier: [number, number, number, number];
```

### inExpo

```ts
inExpo: object;
```

Whip exit: slow start, violent finish.

#### inExpo.cubicBezier

```ts
cubicBezier: [number, number, number, number];
```

### inOutCubic

```ts
inOutCubic: object;
```

Symmetric S-curve for hits and returns.

#### inOutCubic.cubicBezier

```ts
cubicBezier: [number, number, number, number];
```

### inOutSine

```ts
inOutSine: object;
```

Flat, organic S-curve — breathing loops and filmic drifts.

#### inOutSine.cubicBezier

```ts
cubicBezier: [number, number, number, number];
```

### inQuart

```ts
inQuart: object;
```

Faster exit.

#### inQuart.cubicBezier

```ts
cubicBezier: [number, number, number, number];
```

### outBack

```ts
outBack: object;
```

~10% overshoot then settle — the tasteful "pop".

#### outBack.cubicBezier

```ts
cubicBezier: [number, number, number, number];
```

### outCubic

```ts
outCubic: object;
```

Default decelerating fade/entrance.

#### outCubic.cubicBezier

```ts
cubicBezier: [number, number, number, number];
```

### outExpo

```ts
outExpo: object;
```

"The famous one": near-instant attack, very long settle. Hero entrances.

#### outExpo.cubicBezier

```ts
cubicBezier: [number, number, number, number];
```

### outQuint

```ts
outQuint: object;
```

Smooth long-tail entrance (Flow's "smooth" territory).

#### outQuint.cubicBezier

```ts
cubicBezier: [number, number, number, number];
```
