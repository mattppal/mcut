---
title: "ensureFallbackAudioEncoders"
description: Generated TypeScript API reference for the public mcut SDK.
---

[**mcut SDK**](../../../index.mdx)

***

[mcut SDK](../../../index.mdx) / [media/src](../index.mdx) / ensureFallbackAudioEncoders

# Function: ensureFallbackAudioEncoders()

```ts
function ensureFallbackAudioEncoders(): Promise<void>;
```

Defined in: [packages/media/src/encoders.ts:12](https://github.com/mattppal/mcut/blob/main/packages/media/src/encoders.ts#L12)

Register WASM fallback encoders for codecs the runtime cannot encode
natively — today that's AAC on Firefox (no AudioEncoder AAC support), via
`@mediabunny/aac-encoder` (FFmpeg-based). The import is dynamic so
browsers with native AAC never download the WASM. Idempotent; export
paths await it before probing codecs.

## Returns

`Promise`\<`void`\>
