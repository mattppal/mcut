---
title: "PreviewMediaPool"
description: Generated TypeScript API reference for the public mcut SDK.
---

[**mcut SDK**](../../../index.mdx)

***

[mcut SDK](../../../index.mdx) / [media/src](../index.mdx) / PreviewMediaPool

# Class: PreviewMediaPool

Defined in: [packages/media/src/preview-pool.ts:207](https://github.com/mattppal/mcut/blob/main/packages/media/src/preview-pool.ts#L207)

The approximate, low-latency [FrameSource](../../../compositor/src/interfaces/FrameSource.mdx) used for interactive
preview: one pooled `<video>`/`<audio>` element per media asset, kept in
sync with the playback clock, plus decoded `ImageBitmap`s for images.
Audio plays through the media elements themselves (no Web Audio graph);
the deterministic export pipeline is a separate implementation.

Known approximation: two simultaneously-active elements sharing one asset
share one media element, so they render the same source frame.

## Implements

- [`FrameSource`](../../../compositor/src/interfaces/FrameSource.mdx)

## Constructors

### Constructor

```ts
new PreviewMediaPool(resolveAsset): PreviewMediaPool;
```

Defined in: [packages/media/src/preview-pool.ts:216](https://github.com/mattppal/mcut/blob/main/packages/media/src/preview-pool.ts#L216)

#### Parameters

##### resolveAsset

(`assetId`) =>
  \| \{
  `durationMs?`: `number`;
  `hash?`: `string`;
  `height?`: `number`;
  `id`: `` `a-${string}` ``;
  `kind`: `"video"` \| `"audio"` \| `"image"`;
  `mimeType?`: `string`;
  `name?`: `string`;
  `nativePreview?`: `boolean`;
  `src`: `string`;
  `width?`: `number`;
\}
  \| `undefined`

#### Returns

`PreviewMediaPool`

## Methods

### dispose()

```ts
dispose(): void;
```

Defined in: [packages/media/src/preview-pool.ts:379](https://github.com/mattppal/mcut/blob/main/packages/media/src/preview-pool.ts#L379)

#### Returns

`void`

***

### getFrame()

```ts
getFrame(assetId, sourceTimeMs): CanvasImageSource | null;
```

Defined in: [packages/media/src/preview-pool.ts:218](https://github.com/mattppal/mcut/blob/main/packages/media/src/preview-pool.ts#L218)

Image for `assetId` at `sourceTimeMs` (media-local time, after trim).
Return `null` when no frame is available yet; the compositor skips it.

#### Parameters

##### assetId

`` `a-${string}` ``

##### sourceTimeMs

`number`

#### Returns

`CanvasImageSource` \| `null`

#### Implementation of

[`FrameSource`](../../../compositor/src/interfaces/FrameSource.mdx).[`getFrame`](../../../compositor/src/interfaces/FrameSource.mdx#getframe)

***

### pauseAll()

```ts
pauseAll(): void;
```

Defined in: [packages/media/src/preview-pool.ts:373](https://github.com/mattppal/mcut/blob/main/packages/media/src/preview-pool.ts#L373)

Pause everything (e.g. when the player unmounts a project).

#### Returns

`void`

***

### sync()

```ts
sync(items, options): void;
```

Defined in: [packages/media/src/preview-pool.ts:263](https://github.com/mattppal/mcut/blob/main/packages/media/src/preview-pool.ts#L263)

Reconcile pooled media elements with the items active under the
playhead. Called by the playback loop every frame and on seeks.

#### Parameters

##### items

[`ActiveMediaItem`](../interfaces/ActiveMediaItem.mdx)[]

##### options

[`PreviewSyncOptions`](../interfaces/PreviewSyncOptions.mdx)

#### Returns

`void`
