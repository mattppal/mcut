# @mcut/timeline

Headless timeline engine for mcut projects.

```sh
bun add @mcut/timeline
```

This package owns the serializable project model, commands, undo/redo engine,
selectors, effects, transitions, captions, audio metadata, and migration helpers.
Use it anywhere you need deterministic timeline editing without React or browser
rendering dependencies.
