# @mcut/editor

Programmable editor operators for mcut.

```sh
bun add @mcut/editor @mcut/timeline
```

This package builds user-level editing operations on top of `@mcut/timeline`.
Use it for gesture planning, core operators, and higher-level edit behavior that
should stay independent from UI frameworks.
