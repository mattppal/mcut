# @mcut/compositor

Canvas2D compositor for mcut projects.

```sh
bun add @mcut/compositor @mcut/timeline
```

This package renders mcut timeline frames and hit-test geometry for preview and
export flows. It is framework-agnostic and depends on the project model from
`@mcut/timeline`.
