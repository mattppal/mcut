# @mcut/transcription-ai-sdk

Vercel AI SDK transcription provider for mcut.

```sh
bun add @mcut/transcription-ai-sdk @mcut/transcription ai
```

This package normalizes AI SDK transcription results into the shared
`@mcut/transcription` format. The `ai` package is a peer dependency so apps can
choose and configure their transcription model provider.
