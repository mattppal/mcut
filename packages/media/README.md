# @mcut/media

Media I/O helpers for mcut.

```sh
bun add @mcut/media @mcut/timeline @mcut/compositor
```

This package provides probing, thumbnails, audio extraction, preview media, and
browser export helpers built around Mediabunny and WebCodecs-capable runtimes.
It also includes the worker entry used by browser export flows.
