# mcut

Command-line alias for the mcut video editing SDK.

```sh
bunx mcut --help
```

The implementation lives in `@mcut/cli`. Install the scoped package directly
when you want to depend on the CLI library API from another package.
