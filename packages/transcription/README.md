# @mcut/transcription

Provider interface and utilities for mcut transcription.

```sh
bun add @mcut/transcription
```

This package defines normalized transcript data, caption generation helpers, SRT
and VTT output, and commands for applying captions to mcut timeline projects.
