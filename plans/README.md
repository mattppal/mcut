# Implementation Plans

Generated by the improve skill on 2026-06-21. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Restore setup command compatibility and generated Codex config | P1 | S | - | DONE |
| 002 | Make Studio and bridge ports workspace-safe | P1 | M | 001 | DONE |
| 003 | Add live bridge readiness checks and clearer recovery guidance | P2 | M | 001, 002 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency Notes

- 002 depends on 001 because the Conductor setup path must work before port-safe workflow changes can be validated in a fresh workspace.
- 003 depends on 002 because readiness and recovery messages should report the final configured Studio URL, bridge port, and token source.

## Findings Considered And Rejected

- Reintroduce the file-backed `mcut` MCP server in dev config: rejected for this workflow. The transcript and current setup intent explicitly moved local development to a single `mcut-live` server to avoid duplicate tool surfaces.
- Treat the live bridge token `mcut-local-dev` as a security vulnerability: rejected for this local development flow because the bridge binds to `127.0.0.1` and the token is intentionally documented as a local dev token. Plan 002 still makes token/port configuration explicit so the fixed default does not leak into hosted flows.
