---
'@mcut/timeline': patch
'@mcut/compositor': patch
'@mcut/mcp-server': patch
---

A multicam zoom region with no `source` now zooms the whole composite, overlays included, instead of being rejected.
