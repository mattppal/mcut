---
"@mcut/timeline": minor
"@mcut/editor": minor
"@mcut/media": minor
"@mcut/react": patch
---

Video, audio, and multicam clips can store an optional voice cleanup mix. The Clean up voice operator toggles that mix on the current selection in one undo step. Export and preview accept a replacement audio URL per clip so a cleaned stem can stand in for the clip's original audio.
