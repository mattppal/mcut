---
"@mcut/react": minor
"@mcut/compositor": patch
---

Default `PlayerCanvas` preview rendering to the WebGPU compositor where supported, with Canvas2D fallback when WebGPU initialization fails.

Update compositor metadata and docs to describe both the Canvas2D reference renderer and WebGPU backend.
