---
"@mcut/react": patch
---

Add the sync hooks useWebSocket, a reconnecting socket owned by the component lifetime, and useEngineSync, which runs a callback on mount and whenever a selected slice of an engine store changes.
